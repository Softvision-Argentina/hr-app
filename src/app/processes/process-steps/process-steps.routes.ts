import { Routes } from '@angular/router';
import { ProcessStepsComponent } from './process-steps.component';

export const ProcessStepsRoutes: Routes = [
    {
        path: '',
        component: ProcessStepsComponent
    }
];
