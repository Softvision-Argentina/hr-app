import { Routes } from '@angular/router';
import { HireProjectedComponent } from './hire-projected.component';

export const HireProjectedRoutes: Routes = [
  {
    path: '',
    component: HireProjectedComponent
  }
];
